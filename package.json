{
  "name": "banker-lab-pro",
  "version": "6.0.0",
  "private": true,
  "description": "Mobile-first live odds scanner with smart preflight checks, lower-league preference, quota-aware Netlify Functions, and accumulator building.",
  "scripts": {
    "build": "node scripts/build-check.js",
    "start": "netlify dev",
    "preview": "npx serve ."
  },
  "dependencies": {
    "@netlify/functions": "latest"
  },
  "devDependencies": {
    "netlify-cli": "latest",
    "serve": "latest"
  }
}
